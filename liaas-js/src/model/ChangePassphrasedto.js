/**
 * KakrLabs-SDK-Creator
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ChangePassphrasedto model module.
 * @module model/ChangePassphrasedto
 * @version 1.0
 */
class ChangePassphrasedto {
    /**
     * Constructs a new <code>ChangePassphrasedto</code>.
     * @alias module:model/ChangePassphrasedto
     * @param wallet {String} 
     * @param oldPassphrase {String} 
     * @param newPassphrase {String} 
     */
    constructor(wallet, oldPassphrase, newPassphrase) { 
        
        ChangePassphrasedto.initialize(this, wallet, oldPassphrase, newPassphrase);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, wallet, oldPassphrase, newPassphrase) { 
        obj['wallet'] = wallet;
        obj['old_passphrase'] = oldPassphrase;
        obj['new_passphrase'] = newPassphrase;
    }

    /**
     * Constructs a <code>ChangePassphrasedto</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ChangePassphrasedto} obj Optional instance to populate.
     * @return {module:model/ChangePassphrasedto} The populated <code>ChangePassphrasedto</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ChangePassphrasedto();

            if (data.hasOwnProperty('wallet')) {
                obj['wallet'] = ApiClient.convertToType(data['wallet'], 'String');
            }
            if (data.hasOwnProperty('old_passphrase')) {
                obj['old_passphrase'] = ApiClient.convertToType(data['old_passphrase'], 'String');
            }
            if (data.hasOwnProperty('new_passphrase')) {
                obj['new_passphrase'] = ApiClient.convertToType(data['new_passphrase'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ChangePassphrasedto</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ChangePassphrasedto</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ChangePassphrasedto.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['wallet'] && !(typeof data['wallet'] === 'string' || data['wallet'] instanceof String)) {
            throw new Error("Expected the field `wallet` to be a primitive type in the JSON string but got " + data['wallet']);
        }
        // ensure the json data is a string
        if (data['old_passphrase'] && !(typeof data['old_passphrase'] === 'string' || data['old_passphrase'] instanceof String)) {
            throw new Error("Expected the field `old_passphrase` to be a primitive type in the JSON string but got " + data['old_passphrase']);
        }
        // ensure the json data is a string
        if (data['new_passphrase'] && !(typeof data['new_passphrase'] === 'string' || data['new_passphrase'] instanceof String)) {
            throw new Error("Expected the field `new_passphrase` to be a primitive type in the JSON string but got " + data['new_passphrase']);
        }

        return true;
    }


}

ChangePassphrasedto.RequiredProperties = ["wallet", "old_passphrase", "new_passphrase"];

/**
 * @member {String} wallet
 */
ChangePassphrasedto.prototype['wallet'] = undefined;

/**
 * @member {String} old_passphrase
 */
ChangePassphrasedto.prototype['old_passphrase'] = undefined;

/**
 * @member {String} new_passphrase
 */
ChangePassphrasedto.prototype['new_passphrase'] = undefined;






export default ChangePassphrasedto;

